name: Lint Pull Requests

on:
   pull_request:
      branches:
         - main # veya PR hedeflediğin branch

jobs:
   lint:
      runs-on: ubuntu-latest

      steps:
         - name: 📥 Checkout code
           uses: actions/checkout@v3

         - name: ⚙️ Setup Node.js
           uses: actions/setup-node@v3
           with:
              node-version: '20'

         - name: 📦 Install dependencies
           run: npm ci

         - name: 📝 Run lint
           run: npm run lint

         - name: 📝 Check Typescript
           run: npm run check
